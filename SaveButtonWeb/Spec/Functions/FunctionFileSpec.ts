//empty for now the global Office functions prevent the creation of unit tests
describe("A suite", function () {
    it("contains spec with an expectation", function () {
        expect(true).toBe(true);
    });
});